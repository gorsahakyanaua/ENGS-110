diff --git a/homework_2.c b/homework_2.c
index 98a15af..ee3b5ae 100644
--- a/homework_2.c
+++ b/homework_2.c
@@ -1,5 +1,5 @@
 /*
- #include <stdio.h>
+#include <stdio.h>
 
 int main() {
     int number;
@@ -27,9 +27,7 @@ int main() {
 
     return 0;
 }
-
 */
-
 /*
 #include <stdio.h>
 
@@ -71,7 +69,6 @@ int main() {
 
     return 0;
 }
-
 */
 /*
 #include <stdio.h>
@@ -80,7 +77,9 @@ int main() {
 int Prime(int number) {
     if (number <= 1) return 0; // 0 and 1 are not prime numbers
     for (int i = 2; i <= number/2; i++) {
-        if (number % i == 0) return 0; // If it's divisible by any number other than 1 and itself, it's not prime
+        if (number % i == 0) {
+            return 0; // If it's divisible by any number other than 1 and itself, it's not prime
+        }
     }
     return 1; // If no divisor was found, it's prime
 }
@@ -112,8 +111,8 @@ int main() {
 
     return 0;
 }
-
-*//*
+*/
+/*
 #include <stdio.h>
 
 int main() {
@@ -127,7 +126,8 @@ int number2;
 
 printf("The number is %d",number1);
 printf("%d\n",number2);
-}*/
+}
+*/
 /*
 #include <stdio.h>
 
@@ -135,15 +135,15 @@ int main() {
     int number;
     printf("Enter an number: ");
     scanf("%d", &number);
-for(int i = 2;i<=number;i=i*2 ){
-if(number%i!=0 ){
-printf("The number %d not is a power of 2 \n",number);
-return 0;
-}
+    for(int i = 2;i<=number;i=i*2 ){
+        if(number%i!=0 ){
+            printf("The number %d not is a power of 2 \n",number);
+            return 0;
+        }
 
 }
-printf("The number %d is a power of 2\n",number);
-
+    printf("The number %d is a power of 2\n",number);
+    //In your program numbers <= 0 are a power of 2
 }
 */
 /*
@@ -167,10 +167,8 @@ int main() {
 
     return 0;
 }
-
-
 */
-/*
+
 #include <stdio.h>
 
 int main() {
@@ -205,4 +203,3 @@ int main() {
 
     return 0;
 }
-*/
